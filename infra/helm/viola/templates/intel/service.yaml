apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-intel
  labels:
    {{- include "viola.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.intel.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "viola.selectorLabels" (dict "component" "intel" "Release" .Release) | nindent 4 }}
