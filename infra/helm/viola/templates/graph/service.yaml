apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-graph
  labels:
    {{- include "viola.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.graph.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "viola.selectorLabels" (dict "component" "graph" "Release" .Release) | nindent 4 }}
