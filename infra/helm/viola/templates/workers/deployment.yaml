apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-workers
  labels:
    {{- include "viola.labels" . | nindent 4 }}
    {{- include "viola.selectorLabels" (dict "component" "workers" "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.workers.replicaCount }}
  selector:
    matchLabels:
      {{- include "viola.selectorLabels" (dict "component" "workers" "Release" .Release) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "viola.selectorLabels" (dict "component" "workers" "Release" .Release) | nindent 8 }}
    spec:
      serviceAccountName: {{ include "viola.serviceAccountName" . }}
      containers:
        - name: workers
          image: {{ include "viola.image" (dict "global" .Values.global "image" .Values.workers.image "Chart" .Chart) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            {{- include "viola.commonEnv" . | nindent 12 }}
            {{- include "viola.postgresEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.workers.resources | nindent 12 }}
